# Slurm Archive Notes

This directory exists for superseded or annotated Slurm assets that are not meant to be the first thing a new user edits.

The repo still keeps many older top-level `.sbatch` files in place for compatibility, but the canonical human-facing path is now:
- [configs/experiments](/Users/god/Documents/VERL_GRPO/configs/experiments)
- [scripts/sol/submit_experiment.sh](/Users/god/Documents/VERL_GRPO/scripts/sol/submit_experiment.sh)

If a new wrapper becomes non-canonical, move its commentary or snapshot here instead of expanding the active top-level set.
